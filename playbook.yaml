---
- hosts: dev
  become: yes
  tasks:
    - name: ping
      ping:
        data: testing
    - name: copy
      synchronize:
              src: {{ ansible_env.HOME }}/workspace/pupiltalk
              dest: /var/www/

