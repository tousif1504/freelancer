---
- hosts: dev
  become: yes
  tasks:
    - name: ping
      ping:
        data: testing
    - name: copy
      synchronize:
              src: "{{ lookup('env', 'HOME' }}/workspace/pupiltalk"
              dest: "/var/www/"

